# Project assets

This directory contains non-runtime design references, mockups, and media used by the ATHENA project.

- `images/` contains image-based design references that are not served directly by the application.
- `media/` contains PDF or other media references.

Files required by the running website remain in `public/`. JavaScript, CSS, Blade views, and source assets processed by Vite remain in `src/resources/`.
